import React from 'react';
import { Text, View } from 'react-native';

const Layout = () => {
    return (
        <View>
            <Text>Home Screen Layout</Text>
        </View>
    );
};

export default Layout;
